import{_ as t,o as a,c as r,a0 as i}from"./chunks/framework.BQGW-Zpy.js";const p=JSON.parse('{"title":"Security & Privacy","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/security-privacy.md","filePath":"architecture/security-privacy.md"}'),n={name:"architecture/security-privacy.md"};function s(o,e,c,d,l,h){return a(),r("div",null,[...e[0]||(e[0]=[i('<h1 id="security-privacy" tabindex="-1">Security &amp; Privacy <a class="header-anchor" href="#security-privacy" aria-label="Permalink to &quot;Security &amp; Privacy&quot;">​</a></h1><p>The principle running through every design decision here: <strong>selective disclosure</strong>. No personal data, name, phone number, email, IBAN, transaction amount, or the corridor a transfer takes, leaves the system except to a specific, authorized party, under a specific, documented condition, with the disclosure itself logged. Nothing is broadly accessible by default.</p><h2 id="what-never-leaves-the-system-unprotected" tabindex="-1">What never leaves the system unprotected <a class="header-anchor" href="#what-never-leaves-the-system-unprotected" aria-label="Permalink to &quot;What never leaves the system unprotected&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Data</th><th>Where it lives</th><th>Protection</th></tr></thead><tbody><tr><td>Sender/recipient name, email</td><td>Backend database, in transit to the licensed partner</td><td>Encrypted at rest (AES-256) and in transit (TLS 1.2+); never included in logs</td></tr><tr><td>Bank account / IBAN</td><td>Passed to the licensed payout partner only</td><td>Never stored longer than the transfer requires; never anchored on Hedera in raw form</td></tr><tr><td>Card number, CVV</td><td>Should never reach our own servers at all</td><td>See PCI DSS scope reduction below, this is an architecture requirement, not a policy statement</td></tr><tr><td>Transaction amount, corridor, purpose</td><td>Backend database</td><td>Access-controlled; visible to the sender/recipient pair and to compliance review only</td></tr></tbody></table><h2 id="the-anchor-itself-is-already-pseudonymous-by-design-not-by-accident" tabindex="-1">The anchor itself is already pseudonymous, by design, not by accident <a class="header-anchor" href="#the-anchor-itself-is-already-pseudonymous-by-design-not-by-accident" aria-label="Permalink to &quot;The anchor itself is already pseudonymous, by design, not by accident&quot;">​</a></h2><p>Every compliance record anchored on Hedera uses <code>pseudoRef()</code>, a keyed hash of the transaction identifier, not the sender&#39;s or recipient&#39;s actual name, email, or account number. See <code>backend/services/hedera/hashing.mjs</code>. This means:</p><ul><li>The <strong>public, permanent</strong> record (the HCS anchor) never contains raw PII, even though it&#39;s cryptographically verifiable by anyone.</li><li>Reversing a <code>pseudoRef</code> back to the real identity requires the pepper (<code>HEDERA_PSEUDO_PEPPER</code>), held only by this system, not by Hedera, not by anyone inspecting HashScan.</li></ul><p>This is selective disclosure applied to the one piece of this architecture that&#39;s genuinely public: the ledger entry proves a check happened, without exposing who it was about.</p><h2 id="pci-dss-card-data-should-never-touch-our-servers-at-all" tabindex="-1">PCI DSS, card data should never touch our servers at all <a class="header-anchor" href="#pci-dss-card-data-should-never-touch-our-servers-at-all" aria-label="Permalink to &quot;PCI DSS, card data should never touch our servers at all&quot;">​</a></h2><p>Per PCI DSS v4.0.1 (mandatory since 2025, no transition period remaining as of 2026), the cheapest and most secure posture is <strong>scope reduction</strong>: if raw cardholder data never reaches our systems, PCI DSS&#39;s twelve requirements largely don&#39;t apply to us at all.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>The current sandbox <code>Payment.jsx</code> collects card number and CVV directly into its own form for demonstration purposes. <strong>In a production build, this must be replaced with a hosted payment field or tokenized flow from the licensed PSP</strong> (e.g. Stripe Elements, Stripe.js), so the raw card number is submitted directly from the browser to the PSP and this system only ever receives a token. This is an architecture requirement, not an optional hardening step, it is the difference between being in PCI DSS scope and not being in it at all.</p></div><h2 id="gdpr-data-minimization-and-subject-rights" tabindex="-1">GDPR, data minimization and subject rights <a class="header-anchor" href="#gdpr-data-minimization-and-subject-rights" aria-label="Permalink to &quot;GDPR, data minimization and subject rights&quot;">​</a></h2><ul><li><strong>Collect only what&#39;s needed.</strong> The sender email field is optional (see <a href="./design-system.html">Design System</a>) precisely because it isn&#39;t part of the payment route, collecting it as a requirement would violate data minimization for no functional reason.</li><li><strong>Right to erasure and portability</strong> must be implemented at the engineering level, not as a manual process, deletion requests need to reach every data store the record touches (compliance database, any cached copies, payout-partner references), and a pseudonymized Hedera anchor doesn&#39;t need erasure since it never held the raw identity in the first place.</li><li><strong>72-hour breach notification</strong> is a hard regulatory deadline (GDPR Article 33), this needs an incident-response runbook before production, not just encryption.</li></ul><h2 id="selective-disclosure-under-supervision-what-this-means-concretely" tabindex="-1">Selective disclosure under supervision, what this means concretely <a class="header-anchor" href="#selective-disclosure-under-supervision-what-this-means-concretely" aria-label="Permalink to &quot;Selective disclosure under supervision, what this means concretely&quot;">​</a></h2><p>Raw PII is disclosed beyond the sender/recipient pair and the licensed payout partner in exactly two cases, both logged and both requiring a documented legal basis:</p><ol><li><strong>Regulatory or law-enforcement request</strong> (e.g. an AML investigation, a subpoena), disclosed only for the specific transaction named in the request, not as bulk access to the database.</li><li><strong>The licensed partner&#39;s own KYC/AML obligation</strong>, they already need this data to perform their statutory checks; this isn&#39;t an additional disclosure, it&#39;s the minimum data transfer their compliance role structurally requires (see <a href="./../legal/why-this-is-legal.html">Why This Is Legal</a>).</li></ol><p>Nobody else, not an engineer debugging a production issue, not an analytics tool, not a support agent without a specific ticket, gets standing access to raw PII. This is role-based access control (RBAC) plus audit logging on every access, not a policy document; per the 2026 fintech security baseline, &quot;network location grants no implicit trust&quot;, every request authenticates and authorizes individually, including internal ones.</p><h2 id="what-this-means-for-logging-specifically" tabindex="-1">What this means for logging specifically <a class="header-anchor" href="#what-this-means-for-logging-specifically" aria-label="Permalink to &quot;What this means for logging specifically&quot;">​</a></h2><p>A common, easy-to-miss failure mode: an application log line that includes a full name, email, or account number turns every log aggregation tool, every engineer with log access, and every log backup into a PII exposure surface. The practical rule applied throughout this codebase: log the transaction ID and status, never the PII fields themselves. <code>transferRef</code> (the pseudonymous reference) is safe to log; <code>senderName</code> and <code>recipientAccountDetails</code> are not.</p>',19)])])}const m=t(n,[["render",s]]);export{p as __pageData,m as default};
